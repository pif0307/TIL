# 변수의 타입을 숫자타입으로 바꾸는 법(+ 연산자)

변수 앞에 `+`를 붙여주면 숫자 타입으로 변환된다.

```js
var a = "3";
var b = "4";
var c = +a + +b;

console.log(c) // 7
```

## 참고 문서
- [Expressions and operators
](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#Unary_plus_(.2B))